{
  "name": "polyglot",
  "version": "0.1.0",
  "description": "AI-native polyglot trainer (jp/ko/...) that piggybacks on Claude Code / Codex / Gemini workflows",
  "type": "module",
  "module": "src/cli.ts",
  "bin": {
    "lt": "src/cli.ts",
    "jp": "src/cli.ts"
  },
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile dist/lt",
    "install-bin": "bun build src/cli.ts --compile --outfile ~/.local/bin/lt",
    "test": "bun test",
    "dev": "bun run src/cli.ts"
  },
  "dependencies": {
    "ts-fsrs": "^4.6.1",
    "yaml": "^2.6.1",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
